const animales = ['tommy','gato','panson'];

numbers.forEach(function(animal, index) {
    console.log(`Elemento en el índice ${index}: ${animal}`);
});


animales.forEach((animal,index)=>(
  console.log(animal);
  ));
